[project]
name = "cryptarithm"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "nicegui>=3.4.1",
    "fastapi",
    "uvicorn",
    "pymongo",
]

[tool.vercel]
name = "cryptarithm"
entrypoint = "main:app"

[tool.vercel.builds]
image = "python:3.11-slim"

