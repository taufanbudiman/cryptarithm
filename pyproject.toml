[project]
name = "cryptarithm"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "nicegui>=3.4.1",
]

[tool.vercel]
name = "cryptarithm"

[tool.vercel.builds]
image = "python:3.11-slim"

